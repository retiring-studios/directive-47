<Project Sdk="Microsoft.NET.Sdk">

    <!--
        Tier 0. Pure, no dependencies, no platform. net10.0 rather than
        net10.0-windows so this builds and tests in a Linux container.
    -->
    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
    </PropertyGroup>

</Project>
